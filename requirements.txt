# Bjerknes-Diagnostic-Lab — pinned runtime dependencies
#
# Verified: notebooks 01 and 02 both execute end-to-end (Kernel > Restart &
# Run All, no errors) in a fresh virtual environment on Python 3.12 with
# exactly these versions.
#
# Scope: complete for notebooks 01–02 and both physics modules. Notebook 03
# (in progress) carries additional geospatial dependencies (cartopy, netCDF4,
# xarray, pandas, and others); these will be pinned here when it is finalized.
# pandas is intentionally not listed for 01–02 — neither notebook imports it.
#
# Reproduce a clean environment:
#   python3 -m venv .venv
#   source .venv/bin/activate      # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#   pip install notebook           # Jupyter itself, to open the notebooks

numpy==2.5.0
matplotlib==3.11.0
